<section 
    class="mt-32 mx-auto flex flex-col justify-center items-center text-center text-primary"
>
    <h2 
        class="uppercase text-5xl font-semibold"
    >
        12 de septiembre <span aria-hidden class="hidden md:inline">·</span> <br arial-hidden class="block md:hidden"/>
        <span>
            <script is:inline>
                const END_DATE = 1734204784000
                const d = new Date(END_DATE)
                const date = d.toLocaleTimeString("es",{
                    hour: "2-digit",
                    minute: "2-digit"
                })

                
                const tzOffset = d.getTimezoneOffset() / 60
                const diff = tzOffset * -1
                const gmt = diff > 0 ? `GMT+${diff}` : `GMT-${Math.abs(diff)}`

                const TZ_DICTIONARY = {
                    "GMT+1": "CET"
                }

                const tz = TZ_DICTIONARY[gmt] ?? gmt
                
                const selfScript = document.currentScript
                selfScript.parentNode.innerHTML = `${date}H ${gmt}`

            </script>
        </span>

    </h2>
    <h3 
        class="uppercase text-2xl font-medium flex flex-col max-w-sm mt-6"
    >
        <span>Evento de Presentación</span>
        <span>Teatro Victoria (Barcelona)</span>
    </h3>

    <a
        class={`
            border-primary border-2 font-medium px-4 py-1 mt-8 uppercase text-lg hover:bg-gray-300 hover:bg-secondary hover:scale-125
            transition inline-block
        `}
        href="https://www.ticketmaster.es"
        target="_blank"
        rel="noopener noreferrer"
    >
        Entradas agotadas
    </a>
</section>